# Hostrix docs

- Product plan: see [`../PLAN.md`](../PLAN.md)
- Install: [`../installer/install.sh`](../installer/install.sh)

## Phase 1 quick start

1. Start MariaDB
2. Configure env from `.env.example`
3. Run `go run ./cmd/hostrix-api` from `api/`
4. Run `npm run dev` from `panel/`
